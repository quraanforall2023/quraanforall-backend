# Procfile

# This is executed during the release phase before the app is restarted
release: npm install -g @nestjs/cli && npm install && npx prisma generate

# This is executed to start your NestJS application
web: npm start
